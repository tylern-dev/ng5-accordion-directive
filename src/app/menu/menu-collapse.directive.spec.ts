import { MenuCollapseDirective } from './menu-collapse.directive';

describe('MenuCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
